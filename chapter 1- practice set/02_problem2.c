#include<stdio.h>

int main(){
    int r = 6;
    int h=8 ;
    printf("the area of circle is with radius %d is %f\n",r,3.14*r*r);
    printf("the volume of cylinder is with radius %d and height %d is %f",r,h,3.14*r*r*h);
    return 0;
}